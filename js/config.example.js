// Copy this file to config.js and fill in your Mapbox token.
// Obtain a token at https://mapbox.com with STYLES:READ scope.
// For production, restrict the token origin to https://vancouverwalk.com.
// config.js is gitignored and never committed.
const config = {
  mapboxToken: "your_mapbox_token_here",
};
